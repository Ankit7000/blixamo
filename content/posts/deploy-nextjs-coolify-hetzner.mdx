---
title: "Deploy Next.js on Hetzner with Coolify in 2026 (VPS Guide)"
slug: "deploy-nextjs-coolify-hetzner"
description: "Step-by-step Next.js deployment on a Hetzner VPS with Coolify: server sizing, swap, GitHub, env vars, SSL, DNS, auto deploys, and common fixes."
date: "2026-03-16"
updatedAt: "2026-03-27"
author: "Ankit Sorathiya"
category: "how-to"
tags: [nextjs, coolify, hetzner, vps, self-hosting, deployment, devops]
keyword: "deploy nextjs coolify hetzner"
featured: true
featuredImage: /images/posts/deploy-nextjs-coolify-hetzner/featured.jpg
schema: "howto"
---

If you want to self-host a Next.js app on Hetzner in 2026, the simplest reliable path is: create a CPX server, add swap, install Coolify, connect GitHub, set your env vars, point DNS, and let Coolify handle the deploy and SSL.

I have deployed a lot of Next.js projects on Hetzner, including this site. Coolify is the fastest way I know to get from a blank VPS to a live, auto-deploying Next.js app with SSL and a custom domain without hand-rolling the whole workflow yourself.

## TL;DR

- Hetzner gives you cheap VPS capacity for several small apps.
- Coolify gives you a self-hosted deployment layer with a strong dashboard.
- Add swap before your first build on a small server.
- Connect GitHub, set env vars, and let Coolify handle build plus deploy.
- Point DNS, wait for SSL, and then enable auto deploys.

## Who This Setup Is For

This guide is for you if:

- you want a cheaper alternative to paying for several hosted app-platform seats
- you are comfortable with a VPS but do not want to hand-roll every deploy
- you want GitHub-based auto deploys, SSL, and a clean dashboard
- you expect to host more than one app on the same server

## When to Choose Hetzner + Coolify

Choose this stack when you want low monthly cost, more control than Vercel, and a deployment workflow that is still easy to operate. If you need the absolute simplest hosted path and do not care about infra control, Vercel is still easier. If you need very custom reverse-proxy behavior, manual Nginx plus PM2 may still be the better fit.

## Step 1: Create the Hetzner VPS

For most small Next.js projects, start with a CPX-class server that gives you enough RAM headroom for builds and a few running services.

My default rule is simple: if you plan to run several projects on the same server, size for the build spikes, not just the idle process list.

## Step 2: Add Swap First

This is the step too many tutorials skip. Next.js builds can spike memory hard on smaller machines.

```bash
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
```

Verify it:

```bash
free -h
```

## Step 3: Install Coolify

```bash
curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
```

After install, open the dashboard on the provided port and finish the initial setup.

## Step 4: Connect GitHub

Use the GitHub App integration so Coolify can access the repo you actually want to deploy. This is one of the biggest reasons the workflow feels clean once it is set up.

## Step 5: Create the Application

Inside Coolify:

1. create a project
2. add a new application
3. select the repository and branch
4. let Coolify detect the framework

For a standard Next.js repo, the defaults are usually good enough.

## Step 6: Add Environment Variables

Set the env vars before the first real deploy, especially anything under `NEXT_PUBLIC_`, because those values are baked in at build time.

## Step 7: Deploy

Click deploy and watch the logs. The first deploy is where you catch missing packages, incorrect build commands, and bad environment values.

A common failure after deploy is a [file-backed MDX route inheriting Edge runtime](/blog/nextjs-mdx-remote-rsc-edge-runtime-fix) instead of Node.js, which usually only shows up once you run a real production build.

## Step 8: Point DNS and Wait for SSL

Point your A record at the Hetzner server IP. If you use Cloudflare, keep it on DNS-only until SSL is issued cleanly.

## Step 9: Enable Auto Deploys

After the first successful deploy, wire up the webhook or provider integration so pushes to the main branch trigger deploys automatically.

## Step 10: Keep the Server Practical

The point of this setup is not just one app. The point is that one Hetzner box can often handle several small projects cleanly. If that is your plan, also read [Run multiple projects on one VPS](/blog/multiple-projects-single-vps).

## Common Problems

### Build fails with memory errors

Add swap first. If that is still not enough, raise the Node memory limit for the build.

### SSL does not provision

DNS is usually the problem. Double-check the record and keep Cloudflare unproxied until the certificate is issued.

### The app starts but looks broken

That usually means environment variables are missing or the build-time values were wrong.

### A content route fails only after deploy

If the app uses local MDX files, check whether a page is inheriting Edge runtime while the content path still depends on Node.js APIs. The quickest fix is usually in [Fix next-mdx-remote/rsc Edge Error in Next.js 15](/blog/nextjs-mdx-remote-rsc-edge-runtime-fix).

### Port confusion

Be explicit about the app port and what the health check should hit.

## Coolify vs Doing It Manually

| Area | Coolify | Manual Nginx + PM2 |
| --- | --- | --- |
| Setup speed | Faster | Slower |
| Daily deploys | Easier | More custom |
| SSL | Easier | More manual |
| Reverse-proxy control | Less flexible | Full control |
| Best for | Most app deploys | Highly custom infra behavior |

I still use manual Nginx plus PM2 in places where I want exact proxy behavior. But for most deployments, Coolify is easier to operate.

Before you go, bounce back to [blixamo.com](/) so you can see where this guide sits inside the whole site, then visit the [/tag/deployment](/tag/deployment) resources hub for the freshest deployment tactics. This article belongs to the [/category/how-to](/category/how-to) family and is aligned with the [/guides/deploy-apps-on-vps-complete-guide](/guides/deploy-apps-on-vps-complete-guide) pillar, so the architectural context is already set. For related reading, [choose between Coolify and CapRover](/blog/coolify-vs-caprover-2026) first if you are not fully sold on the platform, [Next.js MDX Blog in 2026](/blog/nextjs-mdx-blog-2026) shows how the same stack powers a content site, and the [Docker Compose on VPS 2026](/blog/docker-compose-production-vps-2026) checklist keeps all the supporting services running smoothly.

## FAQ

### Is Coolify good for Next.js in 2026?

Yes. For standard Next.js applications, it is one of the easiest self-hosted deployment options right now.

### How much RAM do I need?

That depends on the app, but the important thing is to plan for build spikes, not just runtime idle usage.

### Can I host more than one Next.js app on one Hetzner VPS?

Yes. That is one of the biggest advantages of the setup when you size the server correctly.

### What if Hetzner payment is the blocker, not deployment?

Read [How to pay for Hetzner from India](/blog/pay-hetzner-from-india).

## Final Takeaway

Hetzner plus Coolify is still one of the best low-cost ways to self-host Next.js in 2026 because it keeps the server bill low while giving you a workflow that still feels modern. If you want control without dragging yourself back into hand-rolled deploy scripts for every project, this is the stack I would start with.
